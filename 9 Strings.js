let name = "Anvith" // This gives primitive
let age = 20

console.log(`My name is ${name.toUpperCase()} and my age is ${age}`)

let newName = new String("Anvith") // This gives object

console.log(name[0])

